# Ansible Configuration

## Prerequisite
Run this ONLY AFTER `terraform apply` has successfully provisioned the underlying infrastructure.

This directory manages all cluster configurations (Namespaces, RBAC, Quotas).
